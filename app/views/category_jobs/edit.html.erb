<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-2">
     <h1 class="text-center text-black">タグ登録（起業希望者ユーザー）</h1> 
     <%= render partial: "layouts/tags/tag_form", locals: { model: @category_job, url: category_job_path, title: "地域について" } %>
     <h5>登録済のタグ</h5>
     <% @category_jobs.each do |category_job| %>
      <div>
       <%= category_job.tag_name %>
    <% end %>
      </div>   
    </div>
  </div> 
</div>   