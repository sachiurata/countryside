<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-2">
     <h1 class="text-center text-black">タグ登録（土の人投稿）</h1>  
     <%= bootstrap_form_with(model: @category_resource, url: category_resource_path, action: :update, local: true) do |f| %>
      <%= f.text_field :tag_name, label:"地域資源" %>
      <%= f.submit "登録" %>
     <% end %>
     
     <h5>登録済のタグ</h5>
     <% @category_resources.each do |category_resource| %>
     <div>
      <%= category_resource.tag_name %>
     <% end %>
     </div> 
    </div>
  </div> 
</div>   