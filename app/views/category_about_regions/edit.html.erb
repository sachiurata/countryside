<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-2">
     <h1 class="text-center text-black">タグ登録（起業希望者投稿）</h1> 
     <%= render partial: "layouts/tag_form", locals: { model: @category_about_region, url: category_about_region_path, title: "地域について" } %>
     <h5>登録済のタグ</h5>
     <% @category_about_regions.each do |category_about_region| %>
      <div>
       <%= category_about_region.tag_name %>
    <% end %>
      </div>   
    </div>
  </div> 
</div>   