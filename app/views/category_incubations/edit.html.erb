<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-2">
     <h1 class="text-center text-black">タグ登録（風の人投稿）</h1> 
     <%= render partial: "layouts/tags/tag_form", locals: { model: @category_incubation, url: category_incubation_path, title: "地域について" } %>
     <h5>登録済のタグ</h5>
     <% @category_incubations.each do |category_incubation| %>
      <div>
       <%= category_incubation.tag_name %>
    <% end %>
      </div>   
    </div>
  </div> 
</div>   